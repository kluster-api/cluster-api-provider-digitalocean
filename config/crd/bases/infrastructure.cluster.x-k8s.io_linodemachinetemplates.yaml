
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.0-beta.0
  creationTimestamp: null
  name: linodemachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: LinodeMachineTemplate
    listKind: LinodeMachineTemplateList
    plural: linodemachinetemplates
    singular: linodemachinetemplate
  scope: Namespaced
  versions:
  - name: v1alpha4
    schema:
      openAPIV3Schema:
        description: LinodeMachineTemplate is the Schema for the linodemachinetemplates
          API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LinodeMachineTemplateSpec defines the desired state of LinodeMachineTemplate.
            properties:
              template:
                description: LinodeMachineTemplateResource describes the data needed
                  to create am LinodeMachine from a template.
                properties:
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the machine.
                    properties:
                      additionalTags:
                        description: AdditionalTags is an optional set of tags to
                          add to Linode resources managed by the Linode provider.
                        items:
                          type: string
                        type: array
                      dataDisks:
                        description: DataDisks specifies the parameters that are used
                          to add one or more data disks to the machine
                        items:
                          description: DataDisk specifies the parameters that are
                            used to add a data disk to the machine.
                          properties:
                            diskSizeGB:
                              description: DiskSizeGB is the size in GB to assign
                                to the data disk.
                              format: int64
                              type: integer
                            filesystemLabel:
                              description: 'FilesystemLabel is the label that is applied
                                to the created filesystem. Character limits apply:
                                16 for ext4; 12 for xfs. May only be used in conjunction
                                with filesystemType.'
                              type: string
                            filesystemType:
                              description: FilesystemType to be used on the volume.
                                When provided the volume will be automatically formatted.
                              type: string
                            nameSuffix:
                              description: NameSuffix is the suffix to be appended
                                to the machine name to generate the disk name. Each
                                disk name will be in format <dropletName>-<nameSuffix>.
                              type: string
                          required:
                          - diskSizeGB
                          - nameSuffix
                          type: object
                        type: array
                      image:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Droplet image can be image id or slug. See https://developers.linode.com/documentation/v2/#list-all-images
                        x-kubernetes-int-or-string: true
                      providerID:
                        description: ProviderID is the unique identifier as specified
                          by the cloud provider.
                        type: string
                      size:
                        description: Droplet size. It must be known Linode droplet
                          size. See https://developers.linode.com/documentation/v2/#list-all-sizes
                        type: string
                      sshKeys:
                        description: SSHKeys is the ssh key id or fingerprint to attach
                          in Linode droplet. It must be available on Linode account.
                          See https://developers.linode.com/documentation/v2/#list-all-keys
                        items:
                          anyOf:
                          - type: integer
                          - type: string
                          x-kubernetes-int-or-string: true
                        type: array
                    required:
                    - image
                    - size
                    - sshKeys
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
